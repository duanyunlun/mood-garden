org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# 关闭 Android SDK 组件的联网自动下载。
# AGP 默认会在构建时联网拉取 SDK 仓库清单（RemoteRepoLoaderImpl.fetchPackages）
# 来校验 / 补装组件；本网络环境下这一步会长时间挂起（实测 400s+ 无任何进展）。
# 所需组件（platform-tools / platforms;android-36 / build-tools;36.0.0 /
# ndk;28.2.13676358）已由 sdkmanager 预先装好，因此可以安全关闭。
# 副作用是正面的：万一真缺组件，会立刻报出「找不到某组件」的明确错误，
# 而不是静默挂起几十分钟。
android.builder.sdkDownload=false
